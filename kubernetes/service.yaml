kind: Service
apiVersion: v1
metadata:
  name: python-web
  namespace: python-web-app
spec:
  selector:
    app: python-web
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
